name: Backfill monthly business-credit charts once

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/business-credit-backfill-once.yml"
      - "backend/sources/paynet_loan_performance.py"
      - "backend/sources/paynet_derived_history.py"
      - "backend/signals/paynet_backfill.py"
      - "tests/test_paynet_loan_performance.py"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: business-credit-backfill-once
  cancel-in-progress: true

jobs:
  backfill:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install --disable-pip-version-check -r backend/requirements.txt
      - name: Resume Equifax legacy history with YoY and MoM reconstruction
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: PYTHONPATH=backend python -m signals.paynet_backfill
