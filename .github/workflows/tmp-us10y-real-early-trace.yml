name: Temporary US10Y REAL early-high trace
on:
  push:
    paths:
      - "tools/tmp_us10y_real_early_trace.py"
      - ".github/workflows/tmp-us10y-real-early-trace.yml"
jobs:
  trace:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.12"}
      - run: pip install requests
      - name: Trace
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: PYTHONPATH=backend python tools/tmp_us10y_real_early_trace.py
