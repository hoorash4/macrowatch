name: Temporary verify stage3 terminal endpoint
on:
  push:
    paths:
      - "backend/historical_pivot_*.py"
      - "tests/test_historical_pivot_stage_boundaries.py"
      - "tools/tmp_verify_stage3_terminal.py"
      - ".github/workflows/tmp-verify-stage3-terminal.yml"
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests pytest matplotlib
      - name: Stage tests
        run: PYTHONPATH=backend pytest -q tests/test_historical_pivot_stage_boundaries.py
      - name: Verify terminal endpoint and render
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: PYTHONPATH=backend python tools/tmp_verify_stage3_terminal.py
      - uses: actions/upload-artifact@v4
        with:
          name: terminal-fix-four
          path: tmp_terminal_fix_four/*.png
