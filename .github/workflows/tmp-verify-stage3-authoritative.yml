name: Temporary verify authoritative stage3 waves
on:
  push:
    paths:
      - "backend/historical_pivot_*.py"
      - "tests/test_historical_pivot_stage_boundaries.py"
      - "tools/tmp_verify_stage3_authoritative.py"
      - ".github/workflows/tmp-verify-stage3-authoritative.yml"
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests pytest matplotlib
      - name: Stage tests
        run: PYTHONPATH=backend pytest -q tests/test_historical_pivot_stage_boundaries.py
      - name: Verify real data and render
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: PYTHONPATH=backend python tools/tmp_verify_stage3_authoritative.py
      - uses: actions/upload-artifact@v4
        with:
          name: authoritative-stage3-four
          path: tmp_stage3_authoritative_four/*.png
