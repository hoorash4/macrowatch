name: Verify Stage6 angle guard temp

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/tmp-verify-stage6-angle.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install --disable-pip-version-check -r backend/requirements.txt
      - name: Run pivot tests
        run: |
          PYTHONPATH=backend python -m unittest tests.test_historical_pivot_stage_boundaries
          PYTHONPATH=backend python -m unittest tests.test_historical_pivot_six_stage_architecture
