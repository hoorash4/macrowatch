name: One-off dispatch Pages
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/oneoff-dispatch-pages.yml'
permissions:
  contents: write
  actions: write
jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Dispatch Pages deploy
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run pages-deploy.yml --ref main
      - name: Remove one-off workflow
        run: |
          git config user.name "hoorash4"
          git config user.email "hoorash4@gmail.com"
          rm .github/workflows/oneoff-dispatch-pages.yml
          git add .github/workflows/oneoff-dispatch-pages.yml
          git commit -m "Remove one-off Pages dispatcher"
          git push
