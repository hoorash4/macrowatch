name: Temporary 4 pivot charts

on:
  push:
    paths:
      - ".github/workflows/tmp-four-pivot-charts.yml"
      - "tools/tmp_render_four_pivot_charts.py"

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests matplotlib
      - name: Render four charts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: PYTHONPATH=backend python tools/tmp_render_four_pivot_charts.py
      - uses: actions/upload-artifact@v4
        with:
          name: four-pivot-charts
          path: tmp_pivot_charts/*.png
