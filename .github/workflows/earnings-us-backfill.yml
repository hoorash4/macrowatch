name: Earnings U.S. earnings backfill

on:
  workflow_dispatch:
    inputs:
      start_year:
        description: Oldest calendar year
        required: true
        default: "2016"
      start_quarter:
        description: Oldest calendar quarter
        required: true
        default: "1"
      end_year:
        description: Newest calendar year
        required: true
        default: "2026"
      end_quarter:
        description: Newest calendar quarter
        required: true
        default: "2"
      write:
        description: Persist each completed period
        required: true
        default: false
        type: boolean
      force_rerun:
        description: Reprocess the requested range even when its checkpoint is complete
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: earnings-us-pipeline
  cancel-in-progress: false

jobs:
  backfill:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install --disable-pip-version-check -r backend/requirements.txt
      - name: Backfill U.S. earnings oldest to newest
        env:
          PYTHONUNBUFFERED: "1"
          PYTHONPATH: backend
          SEC_USER_AGENT: "MacroWatch/1.0 (contact: hoorash44@gmail.com)"
          ECOS_API_KEY: ${{ secrets.ECOS_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        shell: bash
        run: |
          set -euo pipefail
          args=(
            --phase earnings
            --start-year "${{ inputs.start_year }}"
            --start-quarter "${{ inputs.start_quarter }}"
            --end-year "${{ inputs.end_year }}"
            --end-quarter "${{ inputs.end_quarter }}"
          )
          [[ "${{ inputs.write }}" == "true" ]] && args+=(--write)
          [[ "${{ inputs.force_rerun }}" == "true" ]] && args+=(--force-rerun)
          python -m earnings_us.backfill_cli "${args[@]}"

