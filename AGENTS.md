# MacroWatch 작업 지침

## 코드 수정 원칙

- 사용자가 요청하지 않은 자의적 수정, 실행, 범위 확대는 절대 하지 않는다. 제안이 필요하면 먼저 설명하고 승인을 받은 뒤 작업한다.
- 확인하지 않은 추측을 사실처럼 말하지 않는다. 코드·DB·외부 API를 먼저 확인하고, 확인된 사실·추론·불확실한 내용을 명확히 구분해 보고한다.
- 백필은 해당 범위의 기존 DB 값을 유지하거나 보완에 사용하지 않는다. 새 공급자 결과로 전체를 재생성하고, 새 결과의 누락 항목만 보완한 뒤 교체한다.
- 모든 수정은 먼저 버드뷰에서 본다. 관련 모듈, 데이터 흐름, 호출 관계, DB 스키마, 자동화, 프론트엔드 영향을 함께 확인한 뒤 작업한다.
- 거시적인 구조와 전체 흐름을 먼저 확인한 뒤 미시적인 수정 범위를 정하며, 항상 리팩터링한다는 관점에서 수정한다.
- GitHub, Supabase 등 연결된 서비스 작업은 가능한 경우 해당 서비스의 전용 MCP를 우선 사용한다.
- 인앱 브라우저는 전용 MCP, 공식 API 또는 CLI로 처리할 수 없거나 화면을 직접 확인해야 하는 꼭 필요한 경우에만 사용한다.
- 눈앞의 증상만 막는 국지적 덧대기보다 전체 구조를 정리하는 리팩터링을 기본으로 한다.
- 같은 책임이나 값이 여러 곳에 흩어져 있으면 하나의 모듈과 단일 진실 공급원으로 통합한다.
- 새 로직을 추가할 때 기존 로직과 찌꺼기 코드를 함께 정리한다. 임시 우회나 호환 코드가 불가피하면 경계와 제거 조건을 명확히 남긴다.
- 수정 완료는 코드 변경만을 뜻하지 않는다. 관련 DB 계약, 자동 실행 경로, 저장·계산·표시 흐름과 회귀 테스트까지 확인해야 한다.
- 기존 기능과 디자인을 보존하면서 구조를 개선하며, 일부 화면이나 일부 실행 경로만 확인하고 전체 완료로 보고하지 않는다.
