# MacroWatch AGENTS.md

## 0. 최상위 원칙

1. 조사·분석·조회·검증은 자율적으로 수행한다.
2. 실제 변경은 사용자가 요청하거나 승인한 범위 안에서만 수행한다.
3. 승인되지 않은 개선은 수정하지 않고 제안만 한다.
4. 의심스러우면 수정하지 말고 조사·보고한다.
5. 사용자의 명시적인 작업 지시는 해당 범위의 승인으로 간주한다.

**발견과 수정은 별개의 행위다.**
문제나 개선점을 발견했다는 이유만으로 자의적으로 수정하지 않는다.


## 1. 자율 작업과 승인 범위

### 자율적으로 수행할 작업

목적 달성에 필요한 다음 비파괴적 작업은 별도 승인 없이 적극적으로 수행한다.

- GitHub MCP, Supabase MCP 및 기타 사용 가능한 MCP를 이용한 조회
- 앱 브라우저·웹 브라우저를 이용한 공식 문서 및 외부 정보 확인
- 코드·파일·커밋·PR·Issue·diff 검색 및 조회
- 프로젝트 구조와 관련 코드 탐색
- 호출 관계, 데이터 흐름, 의존 관계, 상태 변화 분석
- 로그·오류 메시지·설정·환경 정보 확인
- 라이브러리·패키지·런타임·API 버전 및 호환성 확인
- 문제 해결에 필요한 공식 문서 및 기술 자료 조사
- 외부 상태와 추적 파일을 변경하지 않는 테스트, lint, type check, 임시·격리 build, dry-run
- 원인 및 영향 범위 파악에 필요한 기타 읽기·분석 작업

사용 가능한 도구 중 목적 달성에 가장 적합한 것을 스스로 선택한다.

단, GitHub·Supabase 관련 작업은 **해당 MCP를 우선 사용**한다. MCP로 수행할 수 없거나 필요한 기능이 제공되지 않는 경우에만 앱 브라우저 또는 다른 방법을 사용한다.

### 비파괴적 테스트의 범위

테스트는 다음 조건을 모두 만족한다고 확인된 경우에만 승인 없이 실행할 수 있는 비파괴 작업으로 간주한다.

- 로컬 또는 격리된 테스트 환경에서 실행된다.
- 외부 DB 또는 운영 데이터를 변경하지 않는다.
- 메시지·이메일·푸시·알림 등을 실제 전송하지 않는다.
- 배포나 서비스 상태 변경을 수행하지 않는다.
- 유료 API 또는 비용이 발생할 수 있는 외부 서비스를 호출하지 않는다.
- 외부 시스템에 지속적인 상태 변화를 발생시키지 않는다.
- 저장소의 추적 파일이나 운영 산출물을 변경하지 않는다.

위 조건을 만족하는지 불확실하면 비파괴 작업으로 간주하지 않는다.


## 2. 수정 지시와 승인

사용자가 다음과 같이 실제 작업을 명시적으로 지시한 경우 해당 지시 범위의 변경을 승인한 것으로 본다.

- "수정해"
- "고쳐"
- "적용해"
- "실행해"
- "배포해"
- "병합해"
- 그 밖에 실제 변경 수행을 명확하게 요구하는 표현

이미 명시적으로 지시하거나 승인한 작업에 대해 동일한 승인을 다시 요청하지 않는다.

승인된 작업은 **해당 작업의 완료에 필요한 범위에서** 다음 과정을 하나의 연속된 작업으로 수행한다.

**코드 수정 → 관련 테스트 → 필요한 배포 → DB 및 운영 경로 검증**

각 단계마다 동일한 승인을 반복해서 요청하지 않는다.

단, 작업 도중 최초 지시 또는 승인 범위를 실질적으로 벗어나는 변경이 필요해지면 임의로 범위를 확대하지 않는다. 변경 이유와 영향 범위를 보고하고 추가 승인을 받는다.

### 변경 승인으로 간주하지 않는 표현

다음과 같이 조사·검토·의견·계획만 요구하는 표현은 실제 변경 승인으로 간주하지 않는다.

- "검토해봐"
- "확인해봐"
- "원인을 찾아봐"
- "어때?"
- "가능해?"
- 그 밖에 실제 수정·실행을 명시하지 않은 조사·의견·계획 요청

이 경우 조사·분석·검증·제안까지만 자율적으로 수행하고 실제 변경은 하지 않는다.


## 3. 수정 전 분석과 계획 보고

실제 변경에 들어가기 전에 먼저 충분히 조사하고 다음을 사용자에게 요약한다.

- 근본 원인
- 수정 대상
- 예상 영향 범위
- 적용할 구조적 해결 방법
- 필요한 경우 대안과 장단점

단, 사용자가 이미 구체적인 수정 방법과 범위를 명시하여 지시했고 추가적인 설계 판단이 필요하지 않은 경우 불필요하게 다시 승인을 요청하지 않는다.

### 승인 없이 가능한 기계적 수정

다음처럼 **의도 해석이 필요 없고 기능·동작·구조에 미치는 영향이 사실상 없는 명백한 기계적 수정**은 별도 보고나 승인 없이 즉시 처리할 수 있다.

- 오탈자
- 명백한 문법 오류
- 괄호·쉼표 등 단순 구문 오류
- 명백한 import 누락
- 기능에 영향을 주지 않는 포맷 오류
- 기능에 영향을 주지 않는 단순 lint 오류

이 경우 수정 전 보고는 생략하고 작업 완료 시 자동 수정한 내용을 한 줄로 통보한다.

**사용되지 않는 것으로 보이는 import의 삭제는 자동 수정 대상에 포함하지 않는다.**

lint·type 오류처럼 보여도 로직, 타입 의미, 반환값, 예외 흐름, 설정값, 데이터 구조, 외부 인터페이스 또는 모듈 초기화 등에 영향을 줄 가능성이 있으면 기계적 수정으로 간주하지 않는다.


## 4. 자의적 수정 절대 금지

사용자가 요청하거나 승인하지 않은 변경을 임의로 수행하지 않는다.

다음과 같은 변경은 승인 없이 하지 않는다.

- 기능 추가·삭제
- 기존 동작 변경
- 리팩터링·최적화
- 구조 변경
- 파일 분리·병합
- 변수명·함수명·파일명 변경
- UI·CSS 변경
- 기본값 변경
- 데이터 구조 또는 API 인터페이스 변경
- 패키지 교체·추가·삭제·업그레이드
- 기존 코드 삭제

"더 좋다", "더 깔끔하다", "일반적으로 권장된다", "최신 방식이다", "일관성이 좋아진다"는 이유만으로 변경하지 않는다.

현재 작업과 직접 관계없는 문제나 개선점을 발견하면 자유롭게 조사하고 제안할 수 있지만 실제 수정은 하지 않는다.

**사용자가 요청하지 않은 개선은 실행 대상이 아니라 제안 대상이다.**


## 5. 거시적 구조와 근본 원인 우선

문제를 개별 함수나 오류 한 건만 보고 바로 수정하지 않는다.

먼저 다음을 파악한다.

- 프로젝트 전체 구조
- 주요 모듈·컴포넌트의 역할과 책임
- 호출 관계
- 데이터 흐름
- 상태 및 설정 관리
- DB 및 외부 서비스 연결 구조
- 주요 외부 의존성

현재 문제가 단순한 국소 오류인지 더 큰 구조적 문제에서 파생된 증상인지 구분한다.

수정 위치는 단순히 **"오류가 발생한 위치"**가 아니라 **"해당 책임이 구조적으로 존재해야 하는 위치"**를 기준으로 결정한다.

눈앞의 증상만 막는 다음과 같은 땜질식 해결을 금지한다.

- 특정 사례만을 위한 하드코딩
- 임시 조건문 추가
- 원인 분석 없는 retry
- 원인 분석 없는 sleep 또는 timeout 증가
- 원인 분석 없는 try/except
- 불필요한 fallback 또는 우회 경로

같은 원인이 다시 문제를 만들지 않도록 필요한 범위에서 구조적으로 해결한다.

단, 현재 문제와 무관한 대규모 리팩터링은 하지 않는다.


## 6. 구조화·컴포넌트화·공통화·중앙화

모듈화를 단순한 파일 분리로 해석하지 않는다.

목표는 다음과 같다.

- 책임 명확화
- 중복 제거
- 재사용성 향상
- 변경 지점 최소화

동일하거나 밀접한 기능은 적절한 책임 단위로 그룹화할 수 있는지 검토한다.

반복 기능이나 UI는 컴포넌트화·공통화할 수 있는지 확인한다.

동일한 규칙, 설정, 상수, 스타일, 매핑, 비즈니스 로직은 가능한 경우 하나의 기준점에서 관리한다.

**Single Source of Truth(SSOT)** 를 유지하고 같은 의미의 값이나 정책을 여러 위치에 중복 정의하지 않는다.

CSS, 설정값, 임계값, 경로, 상태 코드, 공통 문자열 등 반복적으로 변경될 요소는 가능한 한 중앙화하여 한 곳의 수정이 전체에 반영되도록 한다.

단순히 파일을 나누기 위한 모듈화, 과도한 추상화, 의미 없는 helper·wrapper 계층은 만들지 않는다.

**모듈화의 목적은 파일 수 증가가 아니라 책임 명확화와 변경 지점 최소화다.**

이러한 구조 개선이 현재 승인된 작업 범위를 벗어나는 경우 실제 변경하지 않고 개선안으로만 보고한다.


## 7. 기존 동작과 영향 범위 보존

승인되지 않은 기존 동작은 보존한다.

특히 다음은 합리적인 이유와 승인 없이 변경하지 않는다.

- 기존 기능
- 변수명·환경변수명
- 데이터 구조
- DB 스키마 및 데이터 의미
- 외부 인터페이스
- 설정값
- UI 동작
- 기존 운영 흐름

구조적 수정이나 공통화를 수행할 때도 기존 의미가 달라지지 않는지 확인한다.

기존 동작을 변경해야만 근본 원인을 해결할 수 있다면 변경 내용과 영향을 사전에 보고하고 승인받는다.


## 8. 임시 조치와 Cleanup

임시 코드나 우회 로직이 필요한 경우 최소 범위로만 사용한다.

최종 코드에 임시 조치를 유지해야 한다면 이유와 후속 작업을 명시한다.

수정 완료 후에는 반드시 Cleanup을 수행한다.

제거 대상은 **이번 작업으로 인해 발생하거나 불필요해진 것**으로 제한한다.

- 이번 작업에서 추가한 임시 코드
- 디버깅 출력
- 테스트용 값
- 임시 조건문·우회 로직
- 이번 수정으로 새롭게 불필요해진 함수·변수·import·분기·설정
- 새 로직으로 대체되어 불필요해진 구버전 코드
- 이번 작업으로 생긴 중복 로직
- 작업 과정에서 생성한 임시 파일

**이번 작업 이전부터 존재하던 무관한 코드, 미사용 코드, 중복 구조 등을 Cleanup 명목으로 자의적으로 수정하거나 삭제하지 않는다.**

별도의 정리가 필요하다고 판단되면 수정하지 않고 제안한다.


## 9. 검증

수정 후에는 문제가 발생했던 경로뿐 아니라 정상 경로도 함께 검증한다.

필요한 경우 다음을 확인한다.

- 경계값
- 빈 값
- 중복 데이터
- 누락 데이터
- 재시도
- 실패 후 복구
- 동일 원인의 재발 가능성
- 관련 DB 저장 결과
- 운영 경로와의 일관성

기존 테스트가 있으면 관련 테스트를 실행하고 필요하면 재발 방지 테스트를 추가한다.

공통화·중앙화·컴포넌트화를 했다면 이를 사용하는 주요 경로에서 동일하게 동작하는지 확인한다.

검증하지 못한 부분은 정상이라고 추측하지 않는다.

검증 과정 자체가 외부 DB 변경, 실제 알림 전송, 유료 API 호출, 배포 또는 외부 시스템 상태 변경을 요구한다면 최초 승인 범위에 포함되는지 확인한다.


## 10. 실행 중 중단 기준

작업 또는 검증 과정에서 다음 상황이 발생하면 즉시 중단하고 사용자에게 보고한다.

- 자동 복구되지 않은 오류 또는 결과의 신뢰성에 영향을 주는 오류가 발생했다.
- 예상하지 못한 데이터 변경 또는 손상 가능성이 발견됐다.
- 승인 범위를 벗어나는 변경이 필요해졌다.
- 정상적인 범위를 벗어나 실행 시간이 비정상적으로 증가했다.
- 외부 서비스 또는 DB에 예상하지 못한 영향을 줄 가능성이 생겼다.
- 현재 방식으로 계속 진행하면 결과의 신뢰성을 보장할 수 없다.

다음은 그 자체만으로 중단 사유로 간주하지 않는다.

- 사전에 설계된 재시도로 정상 복구된 일시적 오류
- 테스트가 의도적으로 발생시킨 오류
- 사전에 합의된 정상적인 `incomplete`
- 정상적인 비동기 처리 대기
- 예상된 데이터 수집 지연 또는 대기 상태


## 11. MacroWatch 데이터 수집 및 백필 원칙

백필과 자동 수집은 **데이터 신뢰 전제와 실행 정책이 다르므로 명확히 구분한다.**

### 백필

백필 대상 범위에서는 기존 결과를 신뢰하거나 보존하지 않는다.

- 기존 DB 데이터가 정확하다고 가정하지 않는다.
- 기존 값을 기준으로 누락 부분만 추정·보완하지 않는다.
- 대상 기간 또는 범위의 외부 원자료를 다시 수집한다.
- 외부 원자료를 기준으로 필요한 변환·검증을 수행한다.
- 검증된 결과로 대상 범위를 전체 교체한다.
- 기존 데이터와 차이가 발생하면 외부 원자료를 우선한다.

### 자동 수집

자동 수집은 이전에 정상적으로 확정된 데이터를 신뢰한다.

- 이전에 확정된 정상 데이터를 임의로 다시 계산하거나 교체하지 않는다.
- 현재 누락된 값 또는 이번 수집 주기의 대상만 지정된 경로로 보완한다.
- 자동 수집용 정상 흐름과 복구 정책을 유지한다.
- 백필 전용 추정·대체·보정·전체 교체 규칙을 자동 수집에 임의로 적용하지 않는다.

백필과 자동 수집의 코드 경로 또는 정책이 분리되어 있다면 그 경계를 유지한다.

사용자가 특정 작업에 대해 별도의 보존·병합·대체 방식을 명시한 경우 그 지시를 우선한다.


## 12. GitHub·Supabase 도구 사용 원칙

GitHub 또는 Supabase와 관련된 작업은 가능한 경우 **MCP를 우선 사용한다.**

### GitHub

다음 작업은 GitHub MCP를 우선한다.

- 파일 및 코드 조회
- 브랜치 확인
- commit 및 diff 확인
- PR·Issue 조회
- 저장소 상태 확인
- 승인된 범위의 commit·push·merge 등 지원되는 변경 작업

### Supabase

다음 작업은 Supabase MCP를 우선한다.

- 테이블·스키마 확인
- SQL 조회
- 데이터 검증
- 로그 및 설정 확인
- 승인된 DB 변경
- 지원되는 프로젝트 관리 작업

MCP에서 필요한 기능을 지원하지 않거나 실제로 수행할 수 없는 경우에만 앱 브라우저 또는 다른 방법을 사용한다.

단순히 익숙하다는 이유로 브라우저를 우선 사용하지 않는다.


## 13. 작업 완료 기준

작업은 단순히 에러가 사라졌다고 완료된 것이 아니다.

다음을 확인한 뒤 완료로 본다.

- 근본 원인이 해결되었다.
- 동일 원인의 재발 가능성을 점검했다.
- 수정 책임이 적절한 위치에 있다.
- 승인되지 않은 동작 변경이 없다.
- 필요한 경우 공통화·중앙화·컴포넌트화가 적용되었다.
- 임시·우회·중복 코드가 정리되었다.
- 정상 경로와 오류 경로를 검증했다.
- 필요한 DB 및 운영 경로 검증이 완료됐다.
- 최종 코드가 불필요하게 복잡해지지 않았다.


## 14. 완료 보고

작업 완료 후에는 간결하게 다음을 보고한다.

1. **원인** — 확인된 근본 원인
2. **변경** — 실제 변경한 핵심 내용
3. **검증** — 수행한 테스트 및 검증 결과
4. **배포/DB** — 수행한 경우 결과
5. **미확인 사항** — 검증하지 못했거나 추가 확인이 필요한 부분
6. **소요 시간** — 실제 작업에 소요된 시간

오탈자나 명백한 기계적 수정만 추가로 처리한 경우에는 별도의 상세 보고 없이 마지막에 한 줄로 통보한다.

완료되지 않은 작업을 완료된 것처럼 보고하지 않는다.

검증하지 않은 내용을 검증했다고 표현하지 않는다.

추정 시간이 아니라 실제 소요 시간을 보고한다.
