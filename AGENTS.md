# MacroWatch AGENTS.md

## 0. 최상위 원칙

- 조사·분석·조회·검증은 자율적으로 수행한다.
- 실제 변경은 사용자가 요청하거나 승인한 범위 안에서만 수행한다.
- 승인되지 않은 개선은 수정하지 않고 제안만 한다.
- 의심스러우면 수정하지 말고 조사·보고한다.
- 문제나 개선점을 발견했다는 이유만으로 자의적으로 수정하지 않는다.

GitHub·Supabase 관련 작업은 해당 MCP를 우선 사용하고, MCP로 수행할 수 없는 경우에만 앱 브라우저나 다른 방법을 사용한다.

## 1. 승인과 자율 작업 범위

다음과 같은 명시적 작업 지시는 해당 범위의 변경 승인으로 본다.

- "수정해"
- "고쳐"
- "적용해"
- "실행해"
- "배포해"
- "병합해"
- 기타 실제 변경 수행을 명확하게 요구하는 표현

반대로 다음과 같은 표현은 변경 승인으로 간주하지 않는다.

- "검토해봐"
- "확인해봐"
- "원인을 찾아봐"
- "어때?"
- "가능해?"
- 기타 조사·의견·계획만 요구하는 표현

승인된 작업은 해당 작업 완료에 필요한 범위에서 **코드 수정 → 관련 테스트 → 필요한 배포 → DB 및 운영 경로 검증**까지 하나의 연속 작업으로 수행하며 단계마다 동일한 승인을 다시 요청하지 않는다.

작업 중 승인 범위를 실질적으로 벗어나는 변경이 필요하면 임의로 확대하지 말고 이유와 영향 범위를 보고한 뒤 추가 승인을 받는다.

### 승인 없이 가능한 작업

목적 달성에 필요한 다음 비파괴적 작업은 별도 승인 없이 적극적으로 수행한다.

- 코드·파일·commit·PR·Issue·diff 조회
- 프로젝트 구조, 호출 관계, 데이터 흐름, 의존성 분석
- 로그·설정·환경 정보 확인
- 공식 문서 및 기술 자료 조사
- 외부 상태와 추적 파일을 변경하지 않는 테스트, lint, type check, 임시·격리 build, dry-run

테스트가 외부 DB·운영 데이터 변경, 실제 알림 전송, 배포, 유료 API 호출 또는 외부 시스템 상태 변경을 포함하면 비파괴 작업으로 간주하지 않는다.

### 승인 없이 가능한 기계적 수정

의도 해석이 필요 없고 기능·동작·구조에 영향이 없는 다음 수정은 즉시 처리할 수 있다.

- 오탈자
- 명백한 문법·구문 오류
- 명백한 import 누락
- 기능에 영향 없는 포맷·단순 lint 오류

이 경우 수정 전 보고는 생략하고 완료 시 한 줄로 통보한다.

사용되지 않는 것으로 보이는 import 삭제나, 타입·반환값·예외 흐름·설정·데이터 구조·외부 인터페이스에 영향을 줄 수 있는 변경은 기계적 수정으로 보지 않는다.

## 2. 자의적 수정 금지와 기존 동작 보존

사용자가 요청하거나 승인하지 않은 다음 변경을 임의로 수행하지 않는다.

- 기능 추가·삭제 또는 동작 변경
- 리팩터링·최적화·구조 변경
- 파일 분리·병합
- 변수명·함수명·파일명 변경
- UI·CSS·기본값 변경
- 데이터 구조·DB 의미·API 인터페이스 변경
- 패키지 교체·추가·삭제·업그레이드
- 기존 코드 삭제

"더 좋다", "더 깔끔하다", "일반적으로 권장된다", "최신 방식이다"라는 이유만으로 변경하지 않는다.

승인되지 않은 기존 기능, 설정, 데이터 의미, 인터페이스, UI 동작, 운영 흐름은 보존한다.

현재 작업과 직접 관계없는 문제나 개선점은 조사·제안만 하고 수정하지 않는다.

**발견과 수정은 별개의 행위다.**

## 3. 거시적 구조·근본 원인·구조화 원칙

실제 변경 전에 근본 원인, 수정 대상, 예상 영향 범위와 구조적 해결 방법을 먼저 조사해 사용자에게 요약한다.

사용자가 이미 구체적인 수정 방법과 범위를 명시했고 추가적인 설계 판단이 필요하지 않다면 동일한 승인을 다시 요청하지 않는다.

개별 오류나 함수만 보고 바로 수정하지 않는다.

먼저 다음을 파악한다.

- 프로젝트 전체 구조
- 주요 모듈·컴포넌트의 책임
- 호출 관계와 데이터 흐름
- 상태·설정·DB·외부 서비스 연결 구조

현재 문제가 국소 오류인지 더 큰 구조적 문제의 증상인지 구분한다.

수정 위치는 **"오류가 발생한 곳"이 아니라 "해당 책임이 구조적으로 존재해야 하는 곳"**을 기준으로 결정한다.

다음과 같은 땜질식 해결은 금지한다.

- 특정 사례만 위한 하드코딩
- 임시 조건문
- 원인 분석 없는 retry·sleep·timeout 증가
- 원인 분석 없는 try/except
- 불필요한 fallback·우회 경로

같은 원인이 재발하지 않도록 필요한 범위에서 구조적으로 해결하되, 현재 문제와 무관한 대규모 리팩터링은 하지 않는다.

### 컴포넌트화·공통화·중앙화

모듈화를 단순한 파일 분리로 해석하지 않는다.

목표는 **책임 명확화, 중복 제거, 재사용성 향상, 변경 지점 최소화**다.

- 동일하거나 밀접한 기능은 적절한 책임 단위로 그룹화한다.
- 반복 기능·UI는 컴포넌트화·공통화 가능한지 검토한다.
- 동일한 규칙, 설정, 상수, 스타일, 매핑, 비즈니스 로직은 가능한 한 하나의 기준점에서 관리한다.
- Single Source of Truth(SSOT)를 유지한다.
- CSS, 설정값, 임계값, 경로, 상태 코드, 공통 문자열 등은 가능한 경우 한 곳에서 수정하면 전체에 반영되도록 중앙화한다.
- 단순 파일 분리, 과도한 추상화, 의미 없는 helper/wrapper 계층은 만들지 않는다.

구조 개선이 승인 범위를 벗어나면 실제 수정하지 않고 제안만 한다.

## 4. 임시 조치·Cleanup·검증

임시 코드나 우회 로직은 필요한 경우 최소 범위로만 사용한다.

최종 코드에 임시 조치를 유지해야 한다면 이유와 후속 작업을 명시한다.

작업 완료 전 반드시 이번 작업으로 인해 생기거나 불필요해진 다음 요소를 정리한다.

- 임시 코드·디버깅 출력·테스트 값
- 임시 조건문·우회 로직·임시 파일
- 새롭게 불필요해진 함수·변수·import·분기·설정
- 새 로직으로 대체된 구버전 코드
- 이번 작업으로 생긴 중복 로직

이번 작업 이전부터 존재하던 무관한 코드나 구조는 Cleanup 명목으로 임의 수정·삭제하지 않는다.

수정 후에는 오류 경로뿐 아니라 정상 경로도 검증하고, 필요한 경우 다음을 확인한다.

- 경계값
- 빈 값
- 중복·누락 데이터
- 재시도
- 실패 후 복구
- 동일 원인의 재발 가능성
- DB 저장 결과와 운영 경로 일관성

기존 관련 테스트를 실행하고, 필요한 경우 동일 원인의 재발 방지 테스트를 추가한다.

검증하지 못한 부분은 정상이라고 추측하지 않는다.

## 5. 실행 중 중단 기준

다음 상황에서는 즉시 중단하고 사용자에게 보고한다.

- 자동 복구되지 않은 오류 또는 결과 신뢰성에 영향을 주는 오류가 발생했다.
- 예상하지 못한 데이터 변경·손상 가능성이 발견됐다.
- 승인 범위를 벗어나는 변경이 필요해졌다.
- 정상 범위를 벗어나 실행 시간이 비정상적으로 증가했다.
- 외부 서비스·DB에 예상하지 못한 영향을 줄 가능성이 생겼다.
- 현재 방식으로 결과의 신뢰성을 보장할 수 없다.

다음은 그 자체만으로 중단 사유가 아니다.

- 설계된 재시도로 정상 복구된 일시 오류
- 테스트가 의도적으로 발생시킨 오류
- 합의된 정상 `incomplete`
- 정상적인 비동기 처리 대기
- 예상된 데이터 수집 지연

## 6. MacroWatch 데이터 원칙

백필과 자동 수집은 데이터 신뢰 전제와 실행 정책을 명확히 구분한다.

### 백필

- 대상 범위의 기존 DB 결과를 신뢰하거나 보존하지 않는다.
- 외부 원자료를 다시 수집해 변환·검증한다.
- 외부 원자료에서 누락된 항목만 사전에 지정된 보완 경로로 채운다.
- 검증된 결과로 대상 범위를 전체 교체한다.
- 기존 데이터와 외부 원자료가 다르면 외부 원자료를 우선한다.

### 자동 수집

- 이전에 정상적으로 확정된 데이터를 신뢰한다.
- 현재 누락된 값 또는 이번 수집 주기의 대상만 지정된 경로로 보완한다.
- 백필 전용 추정·대체·보정·전체 교체 규칙을 자동 수집에 임의로 적용하지 않는다.

백필과 자동 수집의 코드 경로 또는 정책이 분리되어 있다면 그 경계를 유지한다.

사용자가 특정 작업에 별도의 보존·병합·대체 방식을 명시한 경우 그 지시를 우선한다.

## 7. 완료 기준과 보고

다음을 확인한 뒤 완료로 본다.

- 근본 원인이 해결됐다.
- 동일 원인의 재발 가능성을 점검했다.
- 수정 책임이 적절한 위치에 있다.
- 승인되지 않은 동작 변경이 없다.
- 필요한 공통화·중앙화·컴포넌트화가 적용됐다.
- 임시·우회·중복 코드가 정리됐다.
- 정상·오류 경로와 필요한 DB·운영 경로를 검증했다.
- 일부 화면이나 일부 실행 경로만 확인한 상태에서는 전체 작업을 완료로 보고하지 않는다.
- 최종 코드가 불필요하게 복잡해지지 않았다.

완료 후 다음을 간결하게 보고한다.

1. **원인**
2. **변경**
3. **검증**
4. **배포/DB**
5. **미확인 사항**
6. **실제 소요 시간**

완료되지 않은 작업을 완료된 것처럼 보고하지 않고, 검증하지 않은 내용을 검증했다고 표현하지 않는다.
