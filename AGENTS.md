# MacroWatch AGENTS.md

## 0. 최상위 원칙: 자의적 판단 금지 (선승인 후조치 철칙)

**절대 규칙 — 아무리 더 효율적이고, 개선적이며, 안전하고, 자연스러워 보이는 방향이 있더라도, 사용자가 결정한 로직이나 지시한 범위를 넘어서는 순간 절대로 자의적으로 코딩하지 말고 반드시 그 이유와 범위를 명확히 설명한 뒤 선승인을 받는다.**

* **결정된 내용의 실행**: 사용자가 이미 결정한 목표·규칙·수정 방향은 별도의 추가 승인 없이 `코드 수정 → 관련 테스트 → 필요한 배포 → DB 및 운영 경로 검증`까지 자율적으로 끝까지 완결한다.
* **결정되지 않은 영역의 변경**: 진행 과정에서 새로운 조건, 예외, 휴리스틱, 안전장치, 보완 로직, 구조 변경, 동작 재해석이 필요해지면 즉시 멈추고 추가 승인을 받는다.
* **"더 안전하다", "더 깔끔하다", "일반적으로 맞다", "최신 방식이다", "필요해 보인다"는 이유로 사용자가 결정하지 않은 내용을 임의로 코드에 넣는 것을 최우선으로 금지한다.**
* 조사·분석·조회·검증은 자율적으로 수행하되, **발견과 수정은 별개의 행위**다. 문제나 개선점을 발견했다는 이유만으로 자의적으로 수정하지 말고 제안·보고만 한다.
* 모든 작업은 관련 MCP가 있다면 그것을 최우선으로 사용한다.

---

## 1. 자의적 수정 금지 목록 (기존 동작 보존)

사용자가 명시적으로 요청하거나 승인하지 않은 다음 변경은 일절 금지한다.

* 기능 추가·삭제 또는 기존 비즈니스 로직·동작 변경
* 지시받지 않은 리팩터링, 임의 최적화, 파일 분리/병합
* 변수명, 함수명, 파일명, 컴포넌트명 변경
* UI 레이아웃, CSS 스타일, 기본값 변경
* 데이터 모델, DB 스키마/의미, API 인터페이스 변경
* 패키지/라이브러리 추가, 삭제, 교체, 업그레이드
* 기존 코드의 임의 삭제

---

## 2. 5대 클린코드 엔지니어링 원칙

앞으로 작성하거나 수정하는 모든 코드는 아래 5대 원칙을 철저히 준수한다. 지저분한 레거시 코딩 방식을 답습하지 않는다.

1. **압축 코딩 완전 금지 (1줄 1명령 & 표준 포맷팅)**
   * 가독성을 해치는 세미콜론 연타(1줄에 수십 개 명령을 우겨넣은 초고밀도 압축 라인)는 절대 작성하지 않는다.
   * 표준 들여쓰기와 줄바꿈을 엄격히 지켜, 누가 보더라도 3초 만에 흐름과 수정 포인트를 파악할 수 있는 코드로 작성한다.
2. **반복 DOM 수작업 금지 (순수 컴포넌트화)**
   * 기능이 추가될 때마다 절차적으로 `createElement`, `classList.toggle`을 누더기처럼 늘어놓지 않는다.
   * 카드, 모달, 뱃지 등 반복되는 UI는 단일 책임을 갖는 독립된 UI 렌더러 함수(Component)로 공통화·중앙화한다.
3. **관심사의 명확한 분리 (State vs UI vs Data)**
   * 상태(State), 데이터 조회/저장(Repository), 화면 렌더링(Component), 이벤트 흐름(Controller)의 책임을 엄격히 분리한다.
   * 동일한 규칙, 설정, 매핑, 상수는 단일 진실 공급원(SSOT) 한곳에서 중앙 관리한다.
4. **땜질식 예외 처리 금지 (근본 원인 해결)**
   * 에러 상황만 모면하려는 특정 부위 하드코딩, 임시 우회 if문, 원인 분석 없는 수정을 금지한다.
   * 데이터 흐름 전체를 추적하여 근본 원인을 파악하고 구조적으로 해결한다.
5. **작업 완료 전 무조건 클린업 (Clean as you go)**
   * 코드를 추가하는 것보다 정리가 중요하다. 작업 완료 전 이번 작업으로 인해 생기거나 불필요해진 임시 코드, 디버깅 출력, 임시 조건문, 사용되지 않는 함수/변수/import, 구버전 잔재를 반드시 정리한다.

---

## 3. 테스트 및 검증 원칙 (Zero Regression)

* **기존 동작 보존 검증**: 코드를 수정할 때마다 관련 테스트 스위트를 실행하여 기존의 정상 동작이 단 하나도 깨지지 않았음을 기계적으로 검증한다.
* **테스트 실패 시 처리**:
  * "전체 테스트를 통과시켜야 한다"는 이유만으로 기존 테스트의 기대값, fixture, 정책을 임의로 변경하지 않는다.
  * 이번에 사용자가 결정한 변경으로 인해 **직접적으로 깨진 테스트**만 해당 결정의 구현 범위 안에서 함께 수정한다.
  * 인과관계가 명확하지 않거나 별도 기능을 검증하는 테스트가 실패했다면 자의적으로 손대지 말고 원인과 영향 범위를 분석하여 사용자에게 보고 후 결정을 기다린다.
* 검증하지 못한 부분은 정상이라고 추측하지 않는다.

---

## 4. 실행 중 즉시 중단 기준

다음 상황이 발생하면 즉시 작업을 멈추고 사용자에게 현황을 보고한다.

* 자동 복구되지 않거나 결과 신뢰성에 영향을 주는 오류 발생
* 예상하지 못한 데이터 변경, 손상, 유실 가능성 발견
* 사용자의 승인 범위를 벗어나는 새로운 정책, 예외, 로직, 구조 변경이 필요한 순간
* 비정상적인 실행 시간 증가 또는 시스템 교착 상태
* 외부 서비스나 DB에 예상치 못한 부작용을 초래할 위험이 있는 경우

---

## 5. MacroWatch 데이터 원칙

백필과 자동 수집의 신뢰 전제와 코드 경로 경계를 절대 섞지 않는다.

* **백필**: 대상 범위의 기존 DB 데이터를 신뢰하거나 보존하지 않음. 외부 원자료를 다시 수집·검증하여 대상 범위를 전체 교체.
* **자동 수집**: 이전에 정상 확정된 기존 데이터를 신뢰함. 누락된 값 또는 이번 수집 주기의 대상만 지정된 경로로 보완.

---

## 6. 완료 기준과 보고

### 완료 판단 기준
* 근본 원인이 구조적으로 해결되었고, 동일 원인의 재발 가능성이 점검되었다.
* 사용자가 승인하지 않은 동작 변경이 없다.
* 5대 클린코드 원칙(가독성, 컴포넌트화, 관심사 분리, 클린업)이 적용되었다.
* 정상 경로, 오류 경로, 필요한 DB 및 운영 배포 경로가 모두 검증되었다.

### 완료 보고 필수 4개 항목
작업 완료 후 알아듣기 쉽게 아래 4가지 항목으로 보고한다.
1. **변경 사항**
2. **완료 사항**
3. **개선 방향 또는 해결 방법**
4. **실 소요 시간**

* 작업 진행 중 사용자가 말을 걸면 답변하되 작업을 임의로 중단하지 않는다.
* 확실하게 팩트체크를 거치지 않은 내용은 절대 사실처럼 표현하지 않는다.
