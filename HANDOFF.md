# MacroWatch 작업 인수인계

작성일: 2026-08-26 (KST)

## 1. 새 작업에서 가장 먼저 할 일

1. 이 문서를 끝까지 읽는다.
2. `git status --short`와 `git log -5 --oneline`으로 현재 상태를 확인한다.
3. 사용자의 최신 요청 범위만 작업한다.
4. 기존에 정상 동작하는 뉴스 수집·자동 실행·그래프·DB 구조를 요청 없이 수정하지 않는다.
5. 변경 전 관련 코드와 데이터 흐름을 넓게 확인하고, 국지적인 덧대기나 중복 구현을 피한다.

새 작업의 권장 첫 메시지:

> `HANDOFF.md`를 끝까지 읽고 MacroWatch 작업을 이어가자. 먼저 저장소 상태를 확인한 뒤, FOMC 프롬프트 안정성 개선 작업부터 진행해줘. 기존 프롬프트는 임의로 재설계하거나 단순화하지 말고 승인된 부분만 수정해.

## 2. 저장소와 배포 상태

- 로컬 작업 폴더: `C:\Users\hoora\Documents\ChatGPT\MacroWatch`
- GitHub 저장소: `hoorash4/macrowatch`
- 로컬 작업 브랜치: `master`
- 원격 배포 브랜치: `main`
- 현재 최신 커밋: `5ef1915 Refine news classification prompt wording`
- Supabase 프로젝트 ref: `xhghpywvthjuvespzdul`
- Supabase Edge Function은 `main`에 관련 함수 경로가 푸시되면 GitHub Actions가 배포한다.
- 뉴스 자동 분석은 일반 코드 푸시로 실행하지 않는다. 뉴스 실행을 유발하는 특별한 커밋 조건을 함부로 사용하지 않는다.
- 현재 로컬의 `assets/`는 추적되지 않은 사용자 소유 파일이다. 요청 없이 추가·삭제·커밋하지 않는다.

## 3. 사용자의 중요한 작업 원칙

### 최우선 원칙: 국지적 덧대기 금지

- 사용자는 **당장 보이는 오류 한 건만 막는 임시 수정, 조건문·fallback·예외처리 덧붙이기, 같은 기능을 여러 경로에서 따로 고치는 방식**을 강하게 금지한다.
- 여기서 `최소 범위 수정`은 눈앞의 줄만 고친다는 뜻이 아니다. **관련 데이터의 수집 → 변환 → 계산 → 저장 → 조회 → 자동화 전체 흐름을 먼저 확인한 뒤, 근본 원인이 있는 책임 단위 전체를 일관되게 수정한다**는 뜻이다.
- 문제가 반복되거나 기존 구조가 원인을 추적하기 어려울 정도로 복잡하면 기존 코드에 추가 패치를 얹지 않는다. 작업을 멈추고 영향 범위, 중복 경로, 폐기 대상, 새 구조를 먼저 정리해 사용자 승인을 받은 뒤 해당 모듈을 깨끗하게 재구축한다.
- 새 구현과 구 구현을 병행했다면 새 구현 검증 후 구 구현·전용 테스트·워크플로·배포 함수·참조를 한 번에 제거할 수 있도록 처음부터 경계를 분리한다. 사용하지 않는 구버전 찌꺼기를 남기지 않는다.
- API가 값을 주지 않는다고 판단하기 전에 실제 요청 옵션, 응답 원문 구조, 기간 의미, 페이지 처리, 기존 저장값을 확인한다. 공급자 문제로 돌리기 전에 우리 호출과 변환 로직을 먼저 검증한다.
- 단순한 계산을 불필요한 예외·정정 이력·추정 규칙으로 복잡하게 만들지 않는다. 사용자가 정한 목적에 필요한 가장 직접적인 데이터 흐름을 우선한다.
- 작업 완료 보고 전에는 저장소 전체에서 구 경로와 중복 참조를 검색하고, GitHub·Supabase에 배포된 전용 객체까지 확인한다. 로컬 파일만 지운 상태를 완료라고 말하지 않는다.

- 사용자가 요청한 범위를 벗어나 코드·프롬프트·기능·UI·DB·자동화·데이터를 자의적으로 변경하지 않는다.
- 더 좋은 방법처럼 보여도 사용자가 요청하지 않은 변경은 먼저 이유와 영향 범위를 설명하고 승인을 받은 뒤 진행한다.
- 기존 프롬프트는 사용자의 명시적 승인 없이 문장 하나도 삭제·축약·재배치·재설계하지 않는다.
- 프롬프트 수정 요청은 승인된 부분만 최소 범위로 수정하고, 수정 전후 전체 diff를 확인한다.
- 코드 수정도 요청과 직접 관련된 파일·함수·데이터 흐름으로 범위를 제한한다. 주변 코드를 임의로 정리하거나 동작 방식을 바꾸지 않는다.
- 정상 동작하는 수집 코드는 관련 요청이 없으면 건드리지 않는다.
- 문제를 수정할 때 같은 기능을 여러 곳에 덧대거나 `!important` 같은 강제 규칙으로 우회하지 않는다.
- 공통 기능은 기존 모듈을 먼저 확인하고, 필요하면 공통 모듈을 확장한다.
- 사용자 소유의 미관련 변경은 보존한다.
- 변경은 테스트하고 커밋·푸시·배포 확인까지 마친 뒤 결과를 보고한다.
- AI를 실제 실행해 비용이 발생하는 테스트는 필요성과 범위를 먼저 분명히 한다.
- 로그인된 웹 작업이 필요하면 인앱 브라우저를 우선 사용한다.

## 3-1. 앞으로 지켜야 할 코딩 방식

사용자와 합의한 기본 방식은 **기존 구조 확인 → 공통 모듈 확인 → 최소 범위 수정 → 전체 영향 검증** 순서다.

### 수정 전에

1. 요청받은 화면이나 함수만 바로 고치지 말고 관련 호출 흐름·상태·DB·스타일 우선순위를 먼저 확인한다.
2. 같은 기능이나 옵션이 이미 다른 곳에 정의되어 있는지 전체 저장소에서 검색한다.
3. 중복 정의·우선순위 충돌·이전 임시 수정이 원인인지 먼저 찾는다.
4. 정상 동작하는 영역과 사용자 변경을 확인하고 수정 범위를 정한다.
5. 요청 범위를 넘어서는 구조 변경이 필요하면 코드를 바꾸기 전에 사용자에게 설명하고 승인받는다.

### CSS와 디자인

- 색상·폰트·간격·그림자·테두리·카드·버튼 같은 공통 속성은 디자인 토큰과 공통 컴포넌트 모듈에서 관리한다.
- 예를 들어 카드에 그림자가 필요하면 먼저 공통 카드 모듈에 기존 그림자 옵션이 있는지 확인한다.
- 공통 옵션이 없다면 재사용 가능한 옵션을 공통 모듈에 추가한 후 필요한 카드에 적용한다.
- 특정 카드만 달라야 하는 명확한 이유가 있을 때만 국지적인 변형 클래스를 사용한다.
- 요소별 인라인 스타일, 중복 selector, 같은 속성의 반복 정의, 불필요한 `!important`를 만들지 않는다.
- 문제가 생겼다고 더 강한 selector를 추가하기 전에 기존 우선순위 충돌과 중복 정의를 제거한다.
- 메인 헤더처럼 레이아웃상 특별한 영역은 별도 모듈로 관리하되 현재 디자인과 동작을 유지한다.
- 파일을 무조건 잘게 나누기보다 현재 사이트 규모에 맞게 한 스타일 파일 안에서 구역별·역할별 모듈과 상세한 한글 주석으로 관리한다.

### JavaScript·TypeScript·Python

- 기능별 책임을 분리한다. 수집, 변환, 계산, 저장, 응답, UI 렌더링을 하나의 거대한 함수에 섞지 않는다.
- 여러 기능에서 반복되는 검증·정규화·포맷·API 호출·오류 처리는 공통 함수로 만든다.
- 한 곳의 수정이 다른 기능에 영향을 주지 않도록 입력과 출력 계약을 명확히 한다.
- 전역 상태와 암묵적 의존성을 늘리지 않는다.
- 동일한 데이터를 여러 위치에서 서로 다른 방식으로 계산하지 않는다. 기준 계산 함수는 하나만 둔다.
- 재시도·배치·재개 로직은 정상 처리된 데이터를 다시 AI에 보내거나 중복 저장하지 않도록 멱등성을 유지한다.
- 오류를 숨기는 예외 처리나 임시 fallback을 추가하기 전에 실제 원인을 찾는다.
- 코드는 기능 단위로 읽을 수 있게 정리하고, 왜 필요한지와 데이터 흐름을 설명하는 상세한 한글 주석을 유지한다.
- 파일 분리는 파일이 너무 커져 책임 구분이 어려울 때만 한다. 단순히 모듈화라는 이유로 파일 수를 불필요하게 늘리지 않는다.

### DB와 백엔드

- DB 스키마를 바꾸기 전에 기존 테이블·뷰·함수·RLS·관리자 API·프론트 소비처를 모두 확인한다.
- 같은 개념을 나타내는 컬럼이나 테이블을 중복 생성하지 않는다.
- 데이터 마이그레이션은 기존 데이터 보존과 되돌릴 수 있는 경로를 고려한다.
- 저장일·수집일·게시일처럼 의미가 다른 날짜를 섞지 않는다.
- 제외 데이터, 잠정치, 확정치, 최신값의 의미를 DB와 프론트에서 동일하게 유지한다.

### 변경 후

1. diff를 확인해 요청하지 않은 변경이 없는지 검사한다.
2. 관련 단위 테스트와 구조 검사를 실행한다.
3. 기능이 기존과 동일하게 유지돼야 하는 부분은 회귀 테스트한다.
4. GitHub Actions와 Supabase 배포 결과까지 확인한다.
5. 무엇을 바꿨고 무엇을 의도적으로 건드리지 않았는지 사용자에게 보고한다.

### 절대 금지

- 사용자 승인 없이 프롬프트를 더 짧게 만들거나 판단 기준을 추가·삭제·완화·강화하지 않는다.
- 사용자 승인 없이 정상 동작하는 코드의 알고리즘, 수집원, 일정, 저장 기준, 가중치, UI 동작을 바꾸지 않는다.
- 한 문제를 해결한다는 이유로 관련 없는 코드까지 정리하지 않는다.
- 테스트 편의를 위해 실제 자동 실행과 다른 예외 경로를 만들지 않는다.
- 작동 여부를 확인하지 않고 “완료”라고 보고하지 않는다.

## 4. 뉴스 분석 파이프라인 현재 상태

### 최종 뉴스 프롬프트

- 저장소 원본: `supabase/prompts/news-analysis-v1.5.txt`
- Supabase 시크릿: `NEWS_ANALYSIS_SYSTEM_PROMPT`
- 위 파일과 시크릿은 현재 동일한 최종본으로 반영돼 있다.
- 최근 세 문구만 추가 정리한 커밋이 `5ef1915`다.

### 핵심 의미

- `sentiment`는 실제 지수 상승·하락 예측이 아니다.
- 기사 사건이 주가지수에 미치는 영향을 `positive`, `negative`, `neutral`, `uncertain`으로 분류한다.
- 한국 주가지수에 합리적 영향 경로가 있으면 한국에 대한 영향 분류를 우선한다.
- 한국 영향 경로가 없고 미국 영향 경로가 명확할 때만 미국에 대한 영향 분류를 사용한다.
- 한국과 미국에 대한 긍정·부정 영향이 충돌해도 그 이유만으로 `neutral` 또는 `uncertain`을 선택하지 않는다.
- 제외 기사는 감성 집계에서 빠진다. JSON 형식상 `sentiment=neutral`이지만 일반 감성 분류는 수행하지 않는다.
- 제외 여부와 별개로 결정적 뉴스 감지는 계속 수행한다.
- 결정적 위험 상태는 제공된 기사 정보가 단순 가능성·전망이 아니라 현재 존재하는 상태로 보도하는지를 판단한다.
- 입력에 나타나지 않은 근거를 AI가 임의로 추정하면 안 된다.
- `market_context`는 KOSPI에만 적용한다.
- 다이버전스가 없어도 60일 이격도 확대 방향으로 과열·과매도를 검토할 수 있다.

### 관련 코드

- AI 어댑터: `supabase/functions/_shared/openai-adapter.ts`
- KOSPI 지표 계산: `supabase/functions/_shared/market-indicators.ts`
- 뉴스 함수: `supabase/functions/news-pipeline/index.ts`
- 관리자 결정적 뉴스 기준은 `{{EXTREME_SIGNAL_CRITERIA}}` 위치에 데이터로 주입된다.
- AI 응답 누락 시 누락 기사만 재요청하며, 정상 배치 전체를 다시 보내지 않는다.
- 제외 기사는 API 감성 집계에서도 필터링된다.

## 5. 다음 작업: FOMC 프롬프트 안정성 개선

아직 수정하지 않았다. 먼저 현재 `FOMC_POLICY_SYSTEM_PROMPT` 전체를 사용자에게 받아 확인한다. Supabase 시크릿은 저장 후 원문을 다시 읽을 수 없으므로 기억이나 추정으로 재구성하지 않는다.

사용자가 전달한 외부 평가를 검토한 결과, 다음 방향이 타당하다고 판단했다.

### 승인 전 제안 사항

1. `growth_overheat` 범위 축소
   - 단순한 경제·고용·수요·금융여건 강세가 아니다.
   - 그 강세가 정책 정상화·긴축 유지·추가 긴축의 실제 배경으로 기능할 때 사용한다.

2. `insurance_easing`과 `recession_financial_stress` 경계 명확화
   - 단순 성장 둔화 우려나 위험 균형 변화만으로 `recession_financial_stress`를 선택하지 않는다.
   - 경기·고용·신용·유동성·금융안정 악화가 실제 결정의 주요 근거일 때 사용한다.
   - 악화를 선제적으로 완충하는 인하는 `insurance_easing`, 이미 나타난 악화에 대응하는 인하는 `recession_financial_stress`로 보는 기준을 검토한다.

3. `transition_assessment` 정의 정리
   - `confirmed`: 직전 정책 배경과 비교해 명확한 변화
   - `not_confirmed`: 직전 정책 배경과 실질적으로 동일
   - `uncertain`: 직전 정보 부족 또는 변화 여부 판단 불충분
   - 직전 정보가 없을 때 `not_confirmed`로 두지 않는다.

4. `reason_confidence`와 `primary_reason=uncertain` 일관성
   - 제안 기준: `reason_confidence < 0.55`이면 `primary_reason=uncertain`
   - 반대로 `primary_reason=uncertain`이면 `reason_confidence < 0.55`
   - 프롬프트뿐 아니라 저장 전 코드 정규화도 검토한다.
   - 0.55는 운영 기준이므로 상수화하고 향후 조정 가능하게 하는 편이 좋다.

5. 세 가지 `*_mentioned` 필드 의미 축소
   - DB 필드명은 유지하는 편이 안전하다.
   - 단순 상투적·주변적 언급이 아니라 정책 판단·결정 배경·위험 평가와 실질적으로 관련된 언급일 때만 `true`로 한다.

6. 성명문과 보조 데이터의 우선순위
   - 현재 금리 결정과 정책 배경은 FOMC 공식 성명문을 최우선 근거로 사용한다.
   - `meeting_metadata`는 회의 날짜·출처 식별에 사용한다.
   - `previous_policy_context`는 직전 회의와의 비교에만 사용한다.
   - 충돌하면 현재 회의 사실은 공식 성명문을 우선한다.

7. `change_bps`
   - 프롬프트보다 코드 계산이 안정적이다.
   - AI는 현재 목표금리 범위와 action을 추출하고, 코드는 DB의 직전 목표금리와 비교하는 구조를 검토한다.
   - 현재 코드의 `previous_policy_context`에는 직전 목표금리 상·하단이 포함되지 않는다.
   - 2008년 이전 단일 목표금리는 상·하단을 같은 값으로 취급하는 규칙이 필요할 수 있다.
   - 비교에 필요한 값이 없거나 직접 비교할 수 없으면 `null`로 둔다.

### 현재 FOMC 구현의 실제 위치와 주의점

- 함수: `supabase/functions/policy-pipeline/index.ts`
- 프롬프트 시크릿: `FOMC_POLICY_SYSTEM_PROMPT`
- 모델 기본값: `gpt-5.6-luna`
- structured output 스키마는 `RESPONSE_SCHEMA`에 있다.
- 현재 AI 결과는 별도 의미 정규화 없이 JSON 파싱 후 저장된다.
- 현재 `previous_policy_context`에는 직전 회의 날짜, action, primary reason, segment 정보만 전달된다.
- 현재 `change_bps`는 AI 출력값을 저장하고, 50bp 이상 할증 판정에도 사용한다.
- 긴급회의 25%와 50bp 이상 25% 할증은 코드에서 합산되어 최대 50% 할증된다.
- 정책 구간 연속 횟수와 한계체감 계산은 `recomputeSegments()`가 담당한다.
- 이 함수와 DB를 수정할 때 기존 백필 데이터와 할증 계산이 바뀌지 않는지 반드시 검사한다.

## 6. 테스트와 배포 절차

변경 후 최소 검증:

```powershell
C:/Users/hoora/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe -m unittest tests.test_backend
C:/Users/hoora/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe tests/test_dashboard.js
git diff --check
```

관련 파일만 명시적으로 스테이징한다. `assets/` 같은 미관련 파일을 포함하지 않는다.

푸시 후 확인할 GitHub Actions:

- `Verify code structure`
- `Deploy Supabase changes` — Supabase 함수 경로가 바뀐 경우

프롬프트 시크릿을 수정할 때:

- 기존 전체 프롬프트를 먼저 보존한다.
- 승인된 문구만 수정한다.
- Supabase 시크릿 반영 성공을 확인한다.
- 작업용 임시 토큰을 만들었다면 프로젝트와 권한을 최소 범위로 제한하고 작업 직후 삭제한다.

## 7. 주요 프로젝트 파일

- 프론트엔드: `index.html`, `styles.css`, `script.js`, `auth.js`
- 관리자: `admin.html`, `admin.js`
- 코드 구조 설명: `CODE_STRUCTURE.md`
- 뉴스 프롬프트: `supabase/prompts/news-analysis-v1.5.txt`
- 뉴스 함수: `supabase/functions/news-pipeline/index.ts`
- FOMC 함수: `supabase/functions/policy-pipeline/index.ts`
- 관리자 함수: `supabase/functions/admin-control/index.ts`
- 테스트: `tests/test_backend.py`, `tests/test_dashboard.js`

## 8. 현재 미완료 사항

- FOMC 프롬프트 6개 안정성 개선안과 `change_bps` 처리 방식을 사용자의 최종 승인 후 반영해야 한다.
- FOMC 프롬프트 원문을 먼저 확보해야 한다.
- 프롬프트 수정과 코드 정규화는 구분해서 사용자에게 설명하고 승인 범위를 넘기지 않는다.
